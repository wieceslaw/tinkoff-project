springdoc:
  swagger-ui:
    path: "swagger-ui"

management:
  server:
    port: 8080

server:
  port: 8083

app:
  test: 42
  bot:
    name: "tinkoff-bot"
    token: ${BOT_TOKEN}
  scrapper:
    url: "http://localhost:8080"